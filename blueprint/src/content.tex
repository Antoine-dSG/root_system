% In this file you should put the actual content of the blueprint.
% It will be used both by the web and the print version.
% It should *not* include the \begin{document}
%
% If you want to split the blueprint content into several files then
% the current file can be a simple sequence of \input. Otherwise It
% can start with a \section or \chapter for instance.
<<<<<<< Updated upstream

\begin{theorem}
    The 8-tuple $\left(\mathbb{Z}, \mathbb{Z}^l, \left(\mathbb{Z}^l\right)^*, \mathcal{L}, I, \alpha, \alpha^{\vee}, s\right)$ is a root pairing of $A_l$, where:
    \begin{enumerate}
        \item $\mathcal{L}$ is the dot product on $\mathbb{Z}^l \times \left(\mathbb{Z}^l\right)^*$
        \item $I = \{\text{signed intervals on the set }\{1, ..., n\}\} = I^+ \cup I^-$
        \item $s: I \to \text{Perm}(I)$ where for $J \in I$, $s_J=s_{-J}$, $s_J(=K) = -s_J(K)$, defined as $s_J(K)=$
            \[
            \begin{cases}
                J \cup K & \text{if } J \cap K = \emptyset \land J \cup K \in I \\
                K & \text{if } J \cap K = \emptyset \land J \cup K \notin I \\
                K \setminus J & \text{if } J \cap K \neq \emptyset \land J \subset K \land K \setminus J \in I \\
                K & \text{if } J \cap K \neq \emptyset \land J \subset K \land K \setminus J \notin I \\
                -K & \text{if } J \cap K \neq \emptyset \land K \subseteq J \land J \setminus K = \emptyset \\
                -(J\setminus K) & \text{if } J \cap K \neq \emptyset \land K \subseteq J \land J \setminus K \in I \\
                K & \text{if } J \cap K \neq \emptyset \land K \subseteq J \land J \setminus K \notin I \\
                K & \text{if } J \cap K \neq \emptyset \land K \not\subseteq J \land J \not\subset K\\
            \end{cases}
            \]
    \end{enumerate}
\end{theorem}
\begin{proof}
\end{proof}
=======
>>>>>>> Stashed changes
